# Figma Drift Approval Request

| Area | Figma Source | Proposed Deviation | Reason | Risk If Not Changed | Bruce Approval Required |
|---|---|---|---|---|---|
| Affiliate helper | `docs/figma/source/src/app/lib/stay22.ts` | Sanitized copied prototype ID to placeholders, block third-party script loading while IDs are placeholders, and route hotel CTAs to Concierge until Stay22 is configured | Secret/config safety; kickoff requires Doppler placeholders only | Placeholder partner IDs would load external scripts and create fake affiliate links | Yes |
| App architecture | Vite prototype in `docs/figma/source/src/app` | Rebuild as Next/Payload route with seed/Payload data shape | Required stack is Next + Payload, not Vite | Homepage would not prove CMS editability | Yes |
| Route scope | Prototype includes destinations and broader modules | Implement homepage plus phase-one shells only | Homepage-first doctrine blocks broad route expansion | Page count could outrun homepage proof | Yes |
| Fidelity proof | Raw prototype has no comparison screenshots in repo | Create comparison docs and Playwright captures after local route verification | Needed acceptance artifacts are route-specific | Visual review would rely on build success alone | Yes |
| Homepage renderer | `docs/figma/source/src/app/App.tsx` | Wire `/` to the copied Figma homepage stack instead of the simplified `HomepageRenderer` approximation | The active route must match the provided Figma component spine before any polish pass is meaningful | Continued homepage drift; screenshots would prove the wrong design | Yes |
| Global frontend shell | `docs/figma/source/src/app/components/Nav.tsx` and `Footer.tsx` | Suppress generic `SiteHeader` / `SiteFooter` on `/` so Figma `Nav` / `Footer` render as the homepage chrome | The current Next layout injects non-Figma chrome around the homepage | Header/footer drift on every screenshot | Yes |
| Desk rail count | `COMPONENT_LIBRARY.md` Desks spec and `VIAIVE_OPTIMIZATION_V2.md` preserve a five-desk model; source `Desks.tsx` rendered four cards | Added Concierge as the fifth desk tile while preserving the source image-led rail treatment | The package contradicts itself: source says "Five Desks" but provides four cards | Silent omission breaks the five-desk product model and leaves one homepage tile missing | Yes |
| Standard criteria count | `cms-fields.json` requires `viaiveStandard.criteria` min/max 7; source `Standard.tsx` rendered four pillars | Expanded Standard to seven criteria while preserving the source two-column methodology treatment | CMS contract requires seven editable criteria and homepage must prove that model | Four criteria cannot round-trip to the required Payload global without data loss | Yes |
| Atlas model | `COMPONENT_LIBRARY.md` describes seven regional hubs; source `Atlas.tsx` rendered six destination mosaic tiles | Recast Atlas mosaic as seven regional tiles while preserving the source mosaic art direction | The markdown/JSON architecture says Atlas is regional, not only city/destination cards | Destination-only tiles weaken the route/template map and regional CMS model | Yes |
| Canonical desk routes | `Nav.tsx`, `Desks.tsx`, `Footer.tsx` use `/desks/asia-intelligence` and `/desks/concierge`; kickoff canonical routes are `/desks/asia` and `/concierge` | Update route hrefs while preserving visible labels and component structure | Canonical route decisions are explicit project constraints | Broken links and noncanonical route proof | Yes |
| Prototype accessibility gaps | `ACCESSIBILITY_SPEC.md`, `COMPONENT_LIBRARY.md`; source components omit several aria/focus/form details | Add non-visual a11y/test hooks only where required for route proof | A11y is a hard P1 gate, and test hooks do not alter the visible Figma composition | Visual match could pass while keyboard/screen-reader behavior fails | Yes |
| Gold on light surfaces | `DESIGN_SYSTEM.md` uses `#c8a96a`, but contrast spec forbids gold body/small copy on paper; live detector reports 2.0–2.2:1 on white/cream | Use darker gold-ink `#7a6234` only for text on white/paper while preserving `#c8a96a` for dark sections, borders, shadows, and hover signatures | WCAG AA contrast and the package contrast contract conflict with several source component labels | Leaving source unchanged keeps visible Figma color but fails accessibility | Yes |
