// Phase-1 stub. Renders from globals/collections, not block fields.
// Agent 4 will wire the React component; this block may be deleted if
// composition lives entirely in the global/collection record.
import type { Block } from 'payload'

export const AtlasBlock: Block = {
  slug: 'AtlasBlock',
  fields: [],
}
